using System.Collections;
using System.Collections.Generic;
using Szn.Framework.UI;
using UnityEngine;

public class E : UIBase
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
